{
    "actorSpecification": 1,
    "fields": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
            "email": {
                "type": "string",
                "title": "Email",
                "description": "Email address returned by the validation API.",
                "example": "hello@example.com"
            },
            "valid": {
                "type": "boolean",
                "title": "Valid",
                "description": "Validation decision returned by the API.",
                "example": true
            },
            "error": {
                "type": "string",
                "title": "Error",
                "description": "Error message returned by the API for invalid requests.",
                "example": "email must not be empty"
            }
        }
    },
    "views": {
        "overview": {
            "title": "Overview",
            "description": "Validation API responses.",
            "transformation": {
                "fields": [
                    "email",
                    "valid",
                    "error"
                ]
            },
            "display": {
                "component": "table",
                "properties": {
                    "email": {
                        "label": "Email",
                        "format": "text"
                    },
                    "valid": {
                        "label": "Valid",
                        "format": "boolean"
                    },
                    "error": {
                        "label": "Error",
                        "format": "text"
                    }
                }
            }
        }
    }
}
