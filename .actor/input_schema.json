{
    "title": "Email Validation Service Input",
    "description": "Provide one or more emails to validate through the external validation API.",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "emails": {
            "title": "Emails",
            "type": "array",
            "description": "List of email addresses to validate.",
            "items": {
                "type": "string",
                "title": "Email address"
            },
            "prefill": [
                "hello@example.com"
            ]
        }
    },
    "required": [
        "emails"
    ]
}
