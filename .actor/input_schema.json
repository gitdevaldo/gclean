{
    "title": "Email Validation Service Input",
    "description": "Provide emails to validate. Use one item for single validation or multiple items for bulk.",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "emails": {
            "title": "Emails",
            "type": "array",
            "editor": "stringList",
            "description": "List of email addresses to validate.",
            "items": {
                "type": "string"
            }
        }
    },
    "required": [
        "emails"
    ]
}
